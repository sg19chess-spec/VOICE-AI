# Twilio Webhook Configuration

# LiveKit SIP URI (get this from LiveKit Cloud after creating SIP trunk)
LIVEKIT_SIP_URI=sip:your-trunk-id@sip.livekit.cloud

# Port (Railway sets this automatically)
PORT=8080
